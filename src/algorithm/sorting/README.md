# 🏆 정렬 알고리즘 총정리

## O(n²) 정렬 (느림)

| 알고리즘 | 최선 | 평균 | 최악 | 안정성 | 특징 |
|---------|------|------|------|--------|------|
| 버블 | O(n) | O(n²) | O(n²) | ✅ | 교환 많음, 조기 종료 가능 |
| 선택 | O(n²) | O(n²) | O(n²) | ❌ | 교환 적음 (최대 n-1번) |
| 삽입 | O(n) | O(n²) | O(n²) | ✅ | 거의 정렬된 데이터에 빠름 |

## O(n log n) 정렬 (빠름!)

| 알고리즘 | 최선 | 평균 | 최악 | 공간 | 특징 |
|---------|------|------|------|------|------|
| 병합 | O(n log n) | O(n log n) | O(n log n) | O(n) | 항상 일정, 안정 정렬 |
| 퀵 | O(n log n) | O(n log n) | O(n²) | O(log n) | 평균 가장 빠름 |

---

## 📝 정렬 선택 가이드
### 언제 뭘 쓸까?

코테에서: Arrays.sort() 또는 Collections.sort() 쓰세요! (Dual-Pivot QuickSort)
거의 정렬된 데이터: 삽입 정렬
메모리 제한 있을 때: 퀵 정렬 (제자리 정렬)
항상 안정적인 성능: 병합 정렬
안정 정렬 필요할 때: 병합 정렬

---

## 📝 참고사항

- **안정 정렬**: 동일한 값의 순서가 유지됨
- **제자리 정렬**: 추가 메모리를 거의 사용하지 않음
- **O(n log n)**: 대부분의 경우 효율적인 정렬 알고리즘의 시간복잡도